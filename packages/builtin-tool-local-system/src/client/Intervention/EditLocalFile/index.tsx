import { memo } from 'react';

// Desktop local file editing intervention removed for enterprise web-only build
const EditLocalFile = memo(() => null);

EditLocalFile.displayName = 'EditLocalFileIntervention';

export default EditLocalFile;
