import { memo } from 'react';

// Desktop popup guard removed for enterprise web-only build
const TopicInPopupGuard = memo(() => null);

TopicInPopupGuard.displayName = 'TopicInPopupGuard';

export default TopicInPopupGuard;
