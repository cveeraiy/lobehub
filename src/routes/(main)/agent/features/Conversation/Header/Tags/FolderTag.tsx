import { memo } from 'react';

// Desktop-only component removed for enterprise web-only build
const FolderTag = memo(() => null);

FolderTag.displayName = 'TopicFolderTag';

export default FolderTag;
