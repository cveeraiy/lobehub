import { memo } from 'react';

// Desktop-only component removed for enterprise web-only build
const CliTestSection = memo(() => null);

CliTestSection.displayName = 'CliTestSection';

export default CliTestSection;
