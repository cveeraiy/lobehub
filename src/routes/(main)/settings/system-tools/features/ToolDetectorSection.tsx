import { memo } from 'react';

// Desktop-only component removed for enterprise web-only build
const ToolDetectorSection = memo(() => null);

ToolDetectorSection.displayName = 'ToolDetectorSection';

export default ToolDetectorSection;
